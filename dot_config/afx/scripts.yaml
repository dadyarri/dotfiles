local:
  - name: Scripts
    description: Sourcing my scripts (aliases, functions)
    directory: ~/.config/zsh
    plugin:
      env:
        PATH: $(~/bin/getpath)
      sources:
        - aliases.sh
        - keybindings.sh
        - options.sh
        - scripts/*.sh


{{- if and (eq .chezmoi.os "linux") (eq .chezmoi.osRelease.id "cachyos") -}}
#!/bin/sh
set -eu

# Re-run when the declarative standalone tool list changes:
# {{ include "dot_config/update-standalone/tools.toml" | sha256sum }}
"$HOME/.local/bin/update-standalone" --install-missing
{{- end }}
